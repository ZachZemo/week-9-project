language: python
python:
  - "3.7"
  
install:
  - pip install pandas
  - pip install matplotlib
  - pip install seaborn
  - pip install numpy
  - pip install sklearn
  
  script:
  - python3 loading.py
